query SideArticleGridQuery {
  posts (first: 5) {
    nodes {
      title
      slug
      id
      featuredImage {
        node {
          sourceUrl(size: MEDIUM)
          altText
        }
      }
    }
  }
}